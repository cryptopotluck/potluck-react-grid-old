import React, { Fragment } from "react";

import SideNav from "../SideNav";

class Layout = ({ children }) => {
  <Fragment>
    <SideNav />
    <div id="main-content">
      {children}
    </div>
  </Fragmen>
};
